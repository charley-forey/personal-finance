/**
 * @deprecated Legacy loader/table helper — unused. Prefer React Query +
 * PageLoading / PageError / EmptyState / AppPageHeader from the UI kit.
 * Kept only so accidental imports fail loudly in review; do not use in new pages.
 */
export {};
