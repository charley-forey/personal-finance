'use client';

/**
 * @deprecated Unused — page context banners were removed from app pages.
 * Kept as a null stub so any lingering imports fail softly.
 */
export function PageContextBanner() {
  return null;
}
